# Kopiera den här filen till .env och fyll i dina egna värden.
# .env committeras ALDRIG — den finns i .gitignore.

# Mapbox-token — hämtas på https://account.mapbox.com/access-tokens/
# Skapa ett token med scope "styles:read" och "tiles:read".
VITE_MAPBOX_TOKEN=
